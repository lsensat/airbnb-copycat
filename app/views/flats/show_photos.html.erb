<%= link_to(flat_path(@flat), class: "btn-normal") do %>
  <i class="fa-solid fa-chevron-left"></i>
<% end %>

<div class="photos">
  <% @flat.photos.each do |photo| %>
    <%= cl_image_tag photo.key, crop: :fill %>
  <% end %>
</div>
